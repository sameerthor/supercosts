/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: 'https://www.supercosts.com', // replace with your actual domain
  generateRobotsTxt: true,            // (optional) generate robots.txt
  sitemapSize: 5000,                   // (optional) max URLs per sitemap
};
